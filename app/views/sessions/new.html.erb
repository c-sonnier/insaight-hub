<div class="min-h-screen bg-[#f8f9fa] flex items-center justify-center px-4 py-12">
  <div class="w-full max-w-6xl flex flex-col lg:flex-row items-center lg:items-start gap-12 lg:gap-24">
    <!-- Left side: Branding -->
    <div class="lg:w-1/2 space-y-8 text-center lg:text-left">
      <!-- Logo with text -->
      <div class="flex items-center gap-3 justify-center lg:justify-start">
        <%= image_tag "logo_h_100.svg", class: "h-20", alt: "insAIght Hub" %>
        <span class="text-6xl font-medium text-[#10325B] px-4">ins<span class="font-bold text-[#38BDF8]">AI</span>ght Hub</span>
      </div>

      <!-- Tagline -->
      <h1 class="text-4xl lg:text-5xl font-italic text-[#10325B] leading-tight">
        Because understanding<br>beats output
      </h1>

      <!-- Description -->
      <p class="text-lg text-[#64748b]">
        Turn scattered AI output <span class="font-semibold text-[#0f172a]">into shared</span> understanding —<br class="hidden lg:block">
        structured, searchable, and easy to act on.
      </p>

      <!-- Hero illustration -->
      <%= image_tag "hero_illustration.png", class: "w-full max-w-lg mx-auto lg:mx-0", alt: "Scattered AI Output to Shared Understanding" %>
    </div>

    <!-- Right side: Sign in card -->
    <div class="w-full max-w-md">
      <div class="bg-white rounded-2xl shadow-lg p-8">
        <div class="space-y-2 mb-6">
          <h2 class="text-2xl font-semibold text-[#0f172a]">Sign in</h2>
          <p class="text-[#64748b]">Access your insights with your insAIght Hub credentials.</p>
        </div>

        <% if alert = flash[:alert] %>
          <div class="alert alert-error mb-4">
            <span><%= alert %></span>
          </div>
        <% end %>

        <% if notice = flash[:notice] %>
          <div class="alert alert-success mb-4">
            <span><%= notice %></span>
          </div>
        <% end %>

        <%= form_with url: session_url, class: "space-y-5" do |form| %>
          <div>
            <label class="block text-sm font-medium text-[#64748b] mb-2" for="email_address">
              Email address
            </label>
            <%= form.email_field :email_address, required: true, autofocus: true, autocomplete: "username", placeholder: "you@company.com", value: params[:email_address], class: "w-full px-4 py-3 border border-gray-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-[#183C67] focus:border-transparent transition" %>
          </div>

          <div>
            <div class="flex justify-between items-center mb-2">
              <label class="block text-sm font-medium text-[#64748b]" for="password">
                Password
              </label>
              <%= link_to "Forgot password?", new_password_path, class: "text-sm text-[#183C67] hover:text-[#2A5080] transition" %>
            </div>
            <%= form.password_field :password, required: true, autocomplete: "current-password", placeholder: "••••••••", maxlength: 72, class: "w-full px-4 py-3 border border-gray-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-[#183C67] focus:border-transparent transition" %>
          </div>

          <div class="flex items-center gap-2">
            <%= form.check_box :remember_me, class: "w-5 h-5 rounded border-gray-300 text-[#183C67] focus:ring-[#183C67]", checked: true %>
            <%= form.label :remember_me, "Remember me", class: "text-sm text-[#0f172a]" %>
          </div>

          <div>
            <%= form.submit "Sign in", class: "w-full py-3 px-4 bg-[#183C67] hover:bg-[#2A5080] text-white font-medium rounded-xl transition cursor-pointer" %>
          </div>
        <% end %>

        <p class="text-center text-sm text-[#64748b] mt-6">
          No account? <a href="mailto:admin@example.com" class="text-[#183C67] hover:text-[#2A5080] transition">Request access</a>
        </p>
      </div>
    </div>
  </div>
</div>
