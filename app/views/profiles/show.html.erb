<div class="container mx-auto px-4 py-8 max-w-2xl">
  <div class="flex justify-between items-center mb-6">
    <h1 class="text-3xl font-bold">Profile</h1>
    <%= link_to "Edit", edit_profile_path, class: "btn btn-primary btn-sm" %>
  </div>

  <div class="card bg-base-100 shadow-xl">
    <div class="card-body">
      <div class="flex items-center gap-4 mb-6">
        <%= user_avatar_or_initials(@user, size: "w-16") %>
        <div>
          <h2 class="text-xl font-bold"><%= @user.name %></h2>
          <p class="text-base-content/70"><%= @user.email_address %></p>
          <% if @user.admin? %>
            <span class="badge badge-primary badge-sm mt-1">Admin</span>
          <% end %>
        </div>
      </div>

      <div class="divider">API Access</div>

      <div class="form-control">
        <label class="label">
          <span class="label-text font-semibold">API Token</span>
        </label>
        <div class="flex gap-2">
          <div class="input input-bordered flex-1 flex items-center font-mono text-sm overflow-hidden" data-controller="clipboard">
            <span class="truncate" data-clipboard-target="source"><%= @user.api_token %></span>
            <button type="button" class="btn btn-ghost btn-xs ml-auto" data-action="clipboard#copy" data-clipboard-target="button">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z" />
              </svg>
            </button>
          </div>
        </div>
        <label class="label">
          <span class="label-text-alt">Use this token to authenticate API requests</span>
        </label>
      </div>

      <div class="mt-4">
        <%= button_to "Regenerate Token", regenerate_token_profile_path, method: :post,
            class: "btn btn-warning btn-sm",
            data: { turbo_confirm: "Are you sure? This will invalidate your current API token and any applications using it will stop working." } %>
      </div>

      <div class="divider">Account Info</div>

      <dl class="space-y-2">
        <div class="flex justify-between">
          <dt class="text-base-content/70">Member since</dt>
          <dd><%= @user.created_at.strftime("%B %d, %Y") %></dd>
        </div>
        <div class="flex justify-between">
          <dt class="text-base-content/70">Insights</dt>
          <dd><%= @user.insight_items.count %></dd>
        </div>
        <div class="flex justify-between">
          <dt class="text-base-content/70">Published</dt>
          <dd><%= @user.insight_items.published.count %></dd>
        </div>
      </dl>

      <% if @user.insight_items.any? %>
        <div class="divider">Export</div>

        <div class="flex items-center justify-between">
          <div>
            <p class="text-sm text-base-content/70">Download all your insights as a zip file</p>
          </div>
          <%= link_to export_all_insights_profile_path, class: "btn btn-outline btn-sm" do %>
            <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 mr-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4" />
            </svg>
            Export All Insights
          <% end %>
        </div>
      <% end %>

      <div class="divider">MCP Integration</div>

      <div class="space-y-4">
        <p class="text-sm text-base-content/70">
          Connect AI tools like Cursor or Claude Code to Insaight Hub using the Model Context Protocol (MCP).
        </p>

        <div class="space-y-2">
          <div class="flex items-center justify-between">
            <label class="label-text font-semibold">Cursor Setup</label>
          </div>
          <p class="text-sm text-base-content/70">
            Add the following to your <code class="bg-base-200 px-1 rounded">~/.cursor/mcp.json</code> file inside the <code class="bg-base-200 px-1 rounded">"mcpServers"</code> object:
          </p>

          <% mcp_config = {
            "insaight-hub" => {
              "url" => "#{request.base_url}/mcp",
              "transport" => "http",
              "headers" => {
                "Authorization" => "Bearer #{@user.api_token}"
              }
            }
          } %>

          <div class="relative" data-controller="clipboard">
            <pre class="bg-base-200 p-4 rounded-lg text-sm overflow-x-auto font-mono" data-clipboard-target="source"><%= JSON.pretty_generate(mcp_config) %></pre>
            <button type="button" class="btn btn-ghost btn-xs absolute top-2 right-2" data-action="clipboard#copy" data-clipboard-target="button" title="Copy to clipboard">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z" />
              </svg>
            </button>
          </div>

          <div class="alert alert-info text-sm mt-4">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" class="stroke-current shrink-0 w-5 h-5"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
            <div>
              <p class="font-semibold">After adding the configuration:</p>
              <ol class="list-decimal list-inside mt-1 space-y-1">
                <li>Restart Cursor to load the new MCP server</li>
                <li>The Insaight Hub tools will be available in your AI conversations</li>
              </ol>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
